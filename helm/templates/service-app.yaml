apiVersion: v1
kind: Service
metadata:
  name: {{ include "tg_file_collector.fullname" . }}
  labels:
    {{- include "tg_file_collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tg_file_collector.selectorLabels" . | nindent 4 }}
