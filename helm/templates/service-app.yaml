apiVersion: v1
kind: Service
metadata:
  name: {{ include "tg-file-collector.fullname" . }}
  labels:
    {{- include "tg-file-collector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tg-file-collector.selectorLabels" . | nindent 4 }}
