apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tg-file-collector.fullname" . }}-test-connection"
  labels:
    {{- include "tg-file-collector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tg-file-collector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
